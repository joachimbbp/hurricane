[render](earth.jpg)

this was almost entirely vibe coded

visualization of ECMWF weather simulation

To download:
`uv run download.py`

to render the data out as a VDB in a sphere:
`uv run write_sphere.py`
